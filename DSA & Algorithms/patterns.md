1. Tree node - >  (can go both left and right at a time when iterating thats why recursion)
self.val = val
self.left = left
self.right = right

Linkedlist node (can go linear ie one step at a time when iterating)
    self.val = val
    self.next = next 